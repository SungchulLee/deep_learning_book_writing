# T5: 글에서 글로 옮기는 전이 트랜스포머
## 들어가며

T5(글에서 글로 옮기는 전이 트랜스포머)는 모든 자연어 처리 과제를 글에서 글로 옮기는 하나의 꼴로 모은다. 과제마다 다른 구조를 두는 대신 모든 과제를 입력 글에서 출력 글을 만드는 일로 놓아 모형이 놀랄 만큼 두루 쓰이게 한다.

## 글에서 글로 옮기는 틀

T5의 핵심 통찰은 모든 자연어 처리 과제를 글 생성으로 나타낼 수 있다는 것이다.

| 과제 | 입력 | 출력 |
|------|-------|--------|
| 번역 | `translate English to German: That is good.` | `Das ist gut.` |
| 요약 | `summarize: <article>` | `<summary>` |
| 분류 | `sentiment: This movie is great!` | `positive` |
| 질의응답 | `question: Who wrote Harry Potter? context: ...` | `J.K. Rowling` |

이 하나로 모은 꼴이 다음을 가능케 한다.

- 모든 과제에 모형 하나
- 과제 사이의 전이 학습
- 간단한 미세 조정 절차

## 구조

T5는 본디의 인코더-디코더 트랜스포머 구조를 쓴다.

$$
\text{T5} = \text{Encoder} + \text{Decoder}
$$

### 핵심 설계 선택

| 측면 | T5의 선택 | 다른 길 |
|--------|-----------|-------------|
| 구조 | 인코더-디코더 | 디코더만 (GPT) |
| 주의 | 상대 자리 편향 | 절대 위치 인코딩 |
| 정규화 | 앞 정규화 | 뒤 정규화 |
| 활성 | GELU (나중에는 ReLU 변형) | ReLU |
| 임베딩 | 인코더와 디코더가 함께 씀 | 따로 |

### 모형의 크기

| 모형 | 인코더/디코더 층 | $d_{\text{model}}$ | 매개변수 |
|-------|------------------------|---------|------------|
| T5-Small | 6/6 | 512 | 6000만 |
| T5-Base | 12/12 | 768 | 2억 2000만 |
| T5-Large | 24/24 | 1024 | 7억 7000만 |
| T5-3B | 24/24 | 1024 | 30억 |
| T5-11B | 24/24 | 1024 | 110억 |

## 사전 학습: 구간 망가뜨리기

T5는 **구간 망가뜨리기**라는 잡음 없애기 목표를 쓴다.

1. 토큰의 15%를 무작위로 고른다
2. 이어진 구간을 파수 토큰 `<extra_id_0>`, `<extra_id_1>` 따위로 바꾼다
3. 본디 구간을 되살리도록 모형을 학습시킨다

**예:**

```
Original: "Thank you for inviting me to your party last week."
Input: "Thank you <extra_id_0> to your party <extra_id_1> week."
Target: "<extra_id_0> for inviting me <extra_id_1> last"
```

파수 하나마다 토큰 여럿을 맞히므로 BERT의 토큰 단위 가리기보다 효율적이다.

### 사전 학습 목표 견주기

| 목표 | 모형 | 맞히는 토큰 | 양방향인가 |
|-----------|-------|-----------------|----------------|
| 가린 언어 모형 (MLM) | BERT | 입력 토큰의 약 15% | 그렇다 |
| 인과 언어 모형 (CLM) | GPT | 토큰의 100% (저마다 다음을 맞힌다) | 아니다 |
| 구간 망가뜨리기 | T5 | 토큰의 약 15% (구간으로) | 인코더는 그렇다, 디코더는 아니다 |

T5의 구간 망가뜨리기는 BERT의 양방향 이해와 GPT의 생성 능력 사이에서 균형을 잡는다. 인코더는 망가진 입력을 온전한 양방향 맥락으로 처리하고 디코더는 빠진 구간을 자기 회귀로 만들어 낸다.

### 여러 과제 학습

T5는 사전 학습 중에 여러 과제를 섞어 학습하면 아래쪽 성능이 나아짐을 보였다. C4(거대 정제 크롤 말뭉치) 데이터셋이 비지도 사전 학습 신호를 주고, 지도 과제를 비율에 맞추어 섞어 넣을 수 있다.

$$\mathcal{L} = \sum_{\text{task}} w_{\text{task}} \cdot \mathcal{L}_{\text{task}}$$

Flan-T5는 1,800개가 넘는 과제로 지시 조정을 하여 이를 넓혔고, 갖가지 과제를 섞은 학습이 영 예시 일반화를 크게 높임을 보였다.

## PyTorch 구현

```python
import torch
import torch.nn as nn
import torch.nn.functional as F
import math
from typing import Optional, Dict, Tuple, List

class T5Config:
    """T5 모형 설정."""
    
    def __init__(
        self,
        vocab_size: int = 32128,
        d_model: int = 512,
        d_kv: int = 64,
        d_ff: int = 2048,
        num_layers: int = 6,
        num_decoder_layers: int = None,
        num_heads: int = 8,
        relative_attention_num_buckets: int = 32,
        relative_attention_max_distance: int = 128,
        dropout_rate: float = 0.1,
        layer_norm_epsilon: float = 1e-6,
        feed_forward_proj: str = "relu"
    ):
        self.vocab_size = vocab_size
        self.d_model = d_model
        self.d_kv = d_kv
        self.d_ff = d_ff
        self.num_layers = num_layers
        self.num_decoder_layers = num_decoder_layers or num_layers
        self.num_heads = num_heads
        self.relative_attention_num_buckets = relative_attention_num_buckets
        self.relative_attention_max_distance = relative_attention_max_distance
        self.dropout_rate = dropout_rate
        self.layer_norm_epsilon = layer_norm_epsilon
        self.feed_forward_proj = feed_forward_proj

class T5RelativePositionBias(nn.Module):
    """
    T5 방식의 상대 자리 편향.
    
    효율을 위해 양동이에 담은 상대 자리를 쓴다.
    """
    
    def __init__(
        self,
        num_heads: int,
        num_buckets: int = 32,
        max_distance: int = 128,
        is_decoder: bool = False
    ):
        super().__init__()
        
        self.num_heads = num_heads
        self.num_buckets = num_buckets
        self.max_distance = max_distance
        self.is_decoder = is_decoder
        
        self.relative_attention_bias = nn.Embedding(num_buckets, num_heads)
    
    @staticmethod
    def _relative_position_bucket(
        relative_position: torch.Tensor,
        bidirectional: bool = True,
        num_buckets: int = 32,
        max_distance: int = 128
    ) -> torch.Tensor:
        """
        상대 자리 양동이를 셈한다.
        
        먼 자리일수록 양동이 간격이 로그로 벌어진다.
        """
        ret = 0
        n = -relative_position
        
        if bidirectional:
            num_buckets //= 2
            ret += (n < 0).to(torch.long) * num_buckets
            n = torch.abs(n)
        else:
            n = torch.max(n, torch.zeros_like(n))
        
        # 양동이의 절반은 정확한 자리를 위한 것이다
        max_exact = num_buckets // 2
        is_small = n < max_exact
        
        # 나머지 절반은 로그로 커지는 칸을 위한 것이다
        val_if_large = max_exact + (
            torch.log(n.float() / max_exact) /
            math.log(max_distance / max_exact) *
            (num_buckets - max_exact)
        ).to(torch.long)
        
        val_if_large = torch.min(
            val_if_large,
            torch.full_like(val_if_large, num_buckets - 1)
        )
        
        ret += torch.where(is_small, n, val_if_large)
        return ret
    
    def forward(self, query_length: int, key_length: int) -> torch.Tensor:
        """
        상대 자리 편향을 셈한다.
        
        돌려주는 값: [1, num_heads, query_length, key_length]
        """
        device = self.relative_attention_bias.weight.device
        
        # 상대 자리
        context_position = torch.arange(query_length, device=device)[:, None]
        memory_position = torch.arange(key_length, device=device)[None, :]
        relative_position = memory_position - context_position
        
        # 자리를 양동이에 담는다
        relative_position_bucket = self._relative_position_bucket(
            relative_position,
            bidirectional=not self.is_decoder,
            num_buckets=self.num_buckets,
            max_distance=self.max_distance
        )
        
        # 편향 값을 얻는다
        values = self.relative_attention_bias(relative_position_bucket)
        values = values.permute([2, 0, 1]).unsqueeze(0)
        
        return values

class T5Attention(nn.Module):
    """상대 자리 편향을 갖춘 T5 다중 머리 주의."""
    
    def __init__(
        self,
        config: T5Config,
        is_decoder: bool = False,
        has_relative_attention_bias: bool = False
    ):
        super().__init__()
        
        self.is_decoder = is_decoder
        self.has_relative_attention_bias = has_relative_attention_bias
        
        self.d_model = config.d_model
        self.d_kv = config.d_kv
        self.num_heads = config.num_heads
        self.inner_dim = self.num_heads * self.d_kv
        
        # 사영
        self.q = nn.Linear(config.d_model, self.inner_dim, bias=False)
        self.k = nn.Linear(config.d_model, self.inner_dim, bias=False)
        self.v = nn.Linear(config.d_model, self.inner_dim, bias=False)
        self.o = nn.Linear(self.inner_dim, config.d_model, bias=False)
        
        # 상대 자리 편향
        if has_relative_attention_bias:
            self.relative_attention_bias = T5RelativePositionBias(
                num_heads=config.num_heads,
                num_buckets=config.relative_attention_num_buckets,
                max_distance=config.relative_attention_max_distance,
                is_decoder=is_decoder
            )
        
        self.dropout = nn.Dropout(config.dropout_rate)
    
    def forward(
        self,
        hidden_states: torch.Tensor,
        key_value_states: Optional[torch.Tensor] = None,
        position_bias: Optional[torch.Tensor] = None,
        attention_mask: Optional[torch.Tensor] = None,
        past_key_value: Optional[Tuple[torch.Tensor]] = None,
        use_cache: bool = False
    ) -> Tuple[torch.Tensor, Optional[torch.Tensor], Optional[Tuple[torch.Tensor]]]:
        """
        앞먹임.
        
        인수:
            hidden_states: [batch, seq_len, d_model]
            key_value_states: 교차 주의에서 쓰는 인코더의 숨은 상태
            position_bias: 미리 셈해 둔 자리 편향
            attention_mask: 주의 가림
            past_key_value: 점진 디코딩을 위해 담아 둔 KV
            use_cache: 고친 캐시를 돌려줄지 여부
        """
        batch_size, seq_len = hidden_states.shape[:2]
        
        # 자기 주의 또는 교차 주의
        is_cross_attention = key_value_states is not None
        
        # Q를 셈한다
        query_states = self.q(hidden_states)
        query_states = query_states.view(batch_size, -1, self.num_heads, self.d_kv).transpose(1, 2)
        
        # K, V를 셈한다 (교차 주의에서는 인코더에서)
        if is_cross_attention:
            key_states = self.k(key_value_states)
            value_states = self.v(key_value_states)
        else:
            key_states = self.k(hidden_states)
            value_states = self.v(hidden_states)
        
        key_states = key_states.view(batch_size, -1, self.num_heads, self.d_kv).transpose(1, 2)
        value_states = value_states.view(batch_size, -1, self.num_heads, self.d_kv).transpose(1, 2)
        
        # 캐싱을 다룬다
        if past_key_value is not None:
            key_states = torch.cat([past_key_value[0], key_states], dim=2)
            value_states = torch.cat([past_key_value[1], value_states], dim=2)
        
        present_key_value = (key_states, value_states) if use_cache else None
        
        # 주의 점수
        scores = torch.matmul(query_states, key_states.transpose(-2, -1))
        
        # 자리 편향을 더한다
        if position_bias is None and self.has_relative_attention_bias:
            position_bias = self.relative_attention_bias(
                query_length=seq_len,
                key_length=key_states.size(2)
            )
        
        if position_bias is not None:
            scores += position_bias
        
        # 주의 가림을 적용한다
        if attention_mask is not None:
            scores += attention_mask
        
        # 소프트맥스와 드롭아웃
        attn_weights = F.softmax(scores.float(), dim=-1).type_as(scores)
        attn_weights = self.dropout(attn_weights)
        
        # 값에 적용한다
        attn_output = torch.matmul(attn_weights, value_states)
        attn_output = attn_output.transpose(1, 2).contiguous()
        attn_output = attn_output.view(batch_size, -1, self.inner_dim)
        attn_output = self.o(attn_output)
        
        return attn_output, position_bias, present_key_value

class T5LayerFF(nn.Module):
    """T5 순전파 층."""
    
    def __init__(self, config: T5Config):
        super().__init__()
        
        self.DenseReluDense = nn.Sequential(
            nn.Linear(config.d_model, config.d_ff, bias=False),
            nn.ReLU(),
            nn.Dropout(config.dropout_rate),
            nn.Linear(config.d_ff, config.d_model, bias=False)
        )
        self.layer_norm = nn.LayerNorm(config.d_model, eps=config.layer_norm_epsilon)
        self.dropout = nn.Dropout(config.dropout_rate)
    
    def forward(self, hidden_states: torch.Tensor) -> torch.Tensor:
        """앞 정규화 순전파 신경망."""
        normed = self.layer_norm(hidden_states)
        ff_output = self.DenseReluDense(normed)
        return hidden_states + self.dropout(ff_output)

class T5EncoderBlock(nn.Module):
    """T5 인코더 블록."""
    
    def __init__(self, config: T5Config, has_relative_attention_bias: bool = False):
        super().__init__()
        
        self.layer_norm = nn.LayerNorm(config.d_model, eps=config.layer_norm_epsilon)
        self.self_attention = T5Attention(
            config,
            is_decoder=False,
            has_relative_attention_bias=has_relative_attention_bias
        )
        self.feed_forward = T5LayerFF(config)
        self.dropout = nn.Dropout(config.dropout_rate)
    
    def forward(
        self,
        hidden_states: torch.Tensor,
        attention_mask: Optional[torch.Tensor] = None,
        position_bias: Optional[torch.Tensor] = None
    ) -> Tuple[torch.Tensor, Optional[torch.Tensor]]:
        """앞먹임."""
        # 자기 주의
        normed = self.layer_norm(hidden_states)
        attn_output, position_bias, _ = self.self_attention(
            normed,
            position_bias=position_bias,
            attention_mask=attention_mask
        )
        hidden_states = hidden_states + self.dropout(attn_output)
        
        # 순전파 신경망
        hidden_states = self.feed_forward(hidden_states)
        
        return hidden_states, position_bias

class T5DecoderBlock(nn.Module):
    """자기 주의와 교차 주의를 갖춘 T5 디코더 블록."""
    
    def __init__(self, config: T5Config, has_relative_attention_bias: bool = False):
        super().__init__()
        
        self.layer_norm = nn.LayerNorm(config.d_model, eps=config.layer_norm_epsilon)
        
        # 가린 자기 주의
        self.self_attention = T5Attention(
            config,
            is_decoder=True,
            has_relative_attention_bias=has_relative_attention_bias
        )
        
        # 교차 주의
        self.cross_attention_layer_norm = nn.LayerNorm(
            config.d_model, eps=config.layer_norm_epsilon
        )
        self.cross_attention = T5Attention(config, is_decoder=True)
        
        # 순전파 신경망
        self.feed_forward = T5LayerFF(config)
        self.dropout = nn.Dropout(config.dropout_rate)
    
    def forward(
        self,
        hidden_states: torch.Tensor,
        encoder_hidden_states: torch.Tensor,
        attention_mask: Optional[torch.Tensor] = None,
        encoder_attention_mask: Optional[torch.Tensor] = None,
        position_bias: Optional[torch.Tensor] = None,
        encoder_decoder_position_bias: Optional[torch.Tensor] = None,
        past_key_value: Optional[Tuple] = None,
        use_cache: bool = False
    ) -> Tuple:
        """앞먹임."""
        self_attn_past_key_value = past_key_value[:2] if past_key_value else None
        cross_attn_past_key_value = past_key_value[2:] if past_key_value else None
        
        # 자기 주의
        normed = self.layer_norm(hidden_states)
        attn_output, position_bias, present_key_value = self.self_attention(
            normed,
            position_bias=position_bias,
            attention_mask=attention_mask,
            past_key_value=self_attn_past_key_value,
            use_cache=use_cache
        )
        hidden_states = hidden_states + self.dropout(attn_output)
        
        # 교차 주의
        normed = self.cross_attention_layer_norm(hidden_states)
        cross_attn_output, _, cross_present = self.cross_attention(
            normed,
            key_value_states=encoder_hidden_states,
            attention_mask=encoder_attention_mask,
            past_key_value=cross_attn_past_key_value,
            use_cache=use_cache
        )
        hidden_states = hidden_states + self.dropout(cross_attn_output)
        
        # 순전파 신경망
        hidden_states = self.feed_forward(hidden_states)
        
        present = present_key_value + cross_present if use_cache else None
        
        return hidden_states, position_bias, present

class T5Model(nn.Module):
    """온전한 T5 인코더-디코더 모형."""
    
    def __init__(self, config: T5Config):
        super().__init__()
        self.config = config
        
        # 함께 쓰는 임베딩
        self.shared = nn.Embedding(config.vocab_size, config.d_model)
        
        # 부호기
        self.encoder_embed_dropout = nn.Dropout(config.dropout_rate)
        self.encoder_blocks = nn.ModuleList([
            T5EncoderBlock(config, has_relative_attention_bias=(i == 0))
            for i in range(config.num_layers)
        ])
        self.encoder_final_layer_norm = nn.LayerNorm(
            config.d_model, eps=config.layer_norm_epsilon
        )
        
        # 복호기
        self.decoder_embed_dropout = nn.Dropout(config.dropout_rate)
        self.decoder_blocks = nn.ModuleList([
            T5DecoderBlock(config, has_relative_attention_bias=(i == 0))
            for i in range(config.num_decoder_layers)
        ])
        self.decoder_final_layer_norm = nn.LayerNorm(
            config.d_model, eps=config.layer_norm_epsilon
        )
        
        # 언어 모형 머리
        self.lm_head = nn.Linear(config.d_model, config.vocab_size, bias=False)
    
    def encode(
        self,
        input_ids: torch.Tensor,
        attention_mask: Optional[torch.Tensor] = None
    ) -> torch.Tensor:
        """입력 수열을 인코딩한다."""
        hidden_states = self.shared(input_ids)
        hidden_states = self.encoder_embed_dropout(hidden_states)
        
        # 주의 가림을 바꾼다
        if attention_mask is not None:
            extended_mask = (1.0 - attention_mask.unsqueeze(1).unsqueeze(2)) * -1e9
        else:
            extended_mask = None
        
        position_bias = None
        for block in self.encoder_blocks:
            hidden_states, position_bias = block(
                hidden_states,
                attention_mask=extended_mask,
                position_bias=position_bias
            )
        
        hidden_states = self.encoder_final_layer_norm(hidden_states)
        return hidden_states
    
    def decode(
        self,
        decoder_input_ids: torch.Tensor,
        encoder_hidden_states: torch.Tensor,
        encoder_attention_mask: Optional[torch.Tensor] = None,
        past_key_values: Optional[List] = None,
        use_cache: bool = False
    ) -> Tuple[torch.Tensor, Optional[List]]:
        """출력 수열을 디코딩한다."""
        hidden_states = self.shared(decoder_input_ids)
        hidden_states = self.decoder_embed_dropout(hidden_states)
        
        # 디코더를 위한 인과 가림
        seq_len = decoder_input_ids.size(1)
        causal_mask = torch.triu(
            torch.ones(seq_len, seq_len, device=decoder_input_ids.device),
            diagonal=1
        ).bool()
        causal_mask = causal_mask.unsqueeze(0).unsqueeze(0) * -1e9
        
        # 인코더 주의 가림
        if encoder_attention_mask is not None:
            encoder_extended_mask = (
                1.0 - encoder_attention_mask.unsqueeze(1).unsqueeze(2)
            ) * -1e9
        else:
            encoder_extended_mask = None
        
        position_bias = None
        presents = [] if use_cache else None
        
        for i, block in enumerate(self.decoder_blocks):
            past = past_key_values[i] if past_key_values else None
            
            hidden_states, position_bias, present = block(
                hidden_states,
                encoder_hidden_states,
                attention_mask=causal_mask,
                encoder_attention_mask=encoder_extended_mask,
                position_bias=position_bias,
                past_key_value=past,
                use_cache=use_cache
            )
            
            if use_cache:
                presents.append(present)
        
        hidden_states = self.decoder_final_layer_norm(hidden_states)
        return hidden_states, presents
    
    def forward(
        self,
        input_ids: torch.Tensor,
        decoder_input_ids: torch.Tensor,
        attention_mask: Optional[torch.Tensor] = None,
        labels: Optional[torch.Tensor] = None
    ) -> Dict[str, torch.Tensor]:
        """앞먹임."""
        # 부호화
        encoder_hidden_states = self.encode(input_ids, attention_mask)
        
        # 디코딩
        decoder_hidden_states, _ = self.decode(
            decoder_input_ids,
            encoder_hidden_states,
            encoder_attention_mask=attention_mask
        )
        
        # 언어 모형 머리
        logits = self.lm_head(decoder_hidden_states)
        
        # 손실을 계산한다
        loss = None
        if labels is not None:
            loss = F.cross_entropy(
                logits.view(-1, logits.size(-1)),
                labels.view(-1),
                ignore_index=-100
            )
        
        return {'loss': loss, 'logits': logits}

# 사용 예
if __name__ == "__main__":
    config = T5Config(vocab_size=32128, d_model=512, num_layers=6, num_heads=8)
    model = T5Model(config)
    
    # 예제 입력
    input_ids = torch.randint(0, config.vocab_size, (2, 32))
    decoder_input_ids = torch.randint(0, config.vocab_size, (2, 16))
    labels = decoder_input_ids.clone()
    
    outputs = model(input_ids, decoder_input_ids, labels=labels)
    
    print(f"Logits shape: {outputs['logits'].shape}")
    print(f"Loss: {outputs['loss'].item():.4f}")
    print(f"Parameters: {sum(p.numel() for p in model.parameters()):,}")
```

## 과제 접두사

T5는 과제를 가리키는 데 글 접두사를 쓴다.

```python
TASK_PREFIXES = {
    "translation_en_to_de": "translate English to German: ",
    "translation_en_to_fr": "translate English to French: ",
    "summarization": "summarize: ",
    "sentiment": "sentiment: ",
    "question": "question: ",
    "answer_span": "answer: ",
}
```

## T5의 변형

| 변형 | 핵심 변화 | 뜻 |
|---------|-------------|--------------|
| **T5 v1.1** | 사전 학습 중 드롭아웃 없음, GEGLU 활성, 임베딩 함께 쓰지 않음 | 크지 않지만 한결같은 개선 |
| **mT5** | 여러 언어, 101개 언어, mC4로 학습 | 글에서 글로 옮기기를 언어 사이의 과제로 넓힘 |
| **Flan-T5** | 1800개 넘는 과제로 지시 조정 | 영 예시 성능이 크게 나아짐. 가장 좋은 공개 인코더-디코더 모형 |
| **UL2** | 잡음 없애기 섞기 (구간 망가뜨리기, 접두 언어 모형, 인과 언어 모형) | 사전 학습 목표를 하나로 모음 |
| **LongT5** | 국소 주의와 한시적 전역 주의를 더함 | 토큰 1만 6천 개까지의 수열을 다룸 |

### T5가 남긴 것

T5가 트랜스포머의 설계 선택을 하나하나 짚어 본 것은 이 분야에서 가장 두루 갖춘 제거 연구 가운데 하나로 남아 있다. 핵심 발견은 다음과 같다.

- 같은 계산 예산에서 지도 과제는 인코더-디코더가 디코더 전용보다 낫다
- 앞 정규화(아래 층 앞의 층 정규화)가 뒤 정규화보다 안정되게 학습된다
- 구간 망가뜨리기가 토큰 단위 가리기나 언어 모형화보다 매개변수를 아낀다
- 큰 모형이 표본을 더 아낀다. 매개변수를 두 배로 하고 데이터를 반으로 줄여도 성능이 나아질 때가 많다

## 요약

T5가 하나로 모은 글에서 글로 옮기는 틀은 다음과 같다.

1. **간결함**: 모든 자연어 처리 과제에 모형 하나
2. **키울 수 있음**: 매개변수 6000만에서 110억까지 통한다
3. **전이 학습**: 사전 학습이 과제 사이로 옮겨 간다
4. **자유로움**: 프롬프트로 새 과제를 쉽게 더한다

## 참고 문헌

1. Raffel, C., et al. (2020). "Exploring the Limits of Transfer Learning with T5." JMLR.
2. Chung, H., et al. (2022). "Scaling Instruction-Finetuned Language Models."
3. Tay, Y., et al. (2022). "UL2: Unifying Language Learning Paradigms."

---

## 사전 학습 목표 두루 살피기

#### 인과 언어 모형화 (CLM)

GPT, LLaMA를 비롯한 디코더 전용 모형이 쓴다.

$$
\mathcal{L}_{\text{CLM}} = -\sum_{t=1}^{T} \log P(x_t | x_1, \ldots, x_{t-1})
$$

**성질:**

- 한 방향 (왼쪽에서 오른쪽)
- 생성에 자연스럽다
- 학습 설정이 간단하다

```python
import torch
import torch.nn.functional as F

def causal_lm_loss(logits, labels):
    """
    인과 언어 모형화 손실.
    
    인수:
        logits: [batch, seq_len, vocab_size]
        labels: [batch, seq_len]
    """
    # 다음 토큰 맞히기를 위해 민다
    shift_logits = logits[..., :-1, :].contiguous()
    shift_labels = labels[..., 1:].contiguous()
    
    return F.cross_entropy(
        shift_logits.view(-1, shift_logits.size(-1)),
        shift_labels.view(-1),
        ignore_index=-100
    )
```

#### 가린 언어 모형화 (MLM)

BERT, RoBERTa를 비롯한 인코더 전용 모형이 쓴다.

$$
\mathcal{L}_{\text{MLM}} = -\sum_{i \in \mathcal{M}} \log P(x_i | \mathbf{x}_{\backslash\mathcal{M}})
$$

**가리는 방식 (BERT):**

- 토큰의 15%를 고른다
- 80%는 [MASK]으로 바꾼다
- 10%는 무작위 토큰으로 바꾼다
- 10%는 그대로 둔다

```python
import torch
import torch.nn as nn

def create_mlm_data(input_ids, vocab_size, mask_token_id, mask_prob=0.15):
    """가린 언어 모형 학습 데이터를 만든다."""
    labels = input_ids.clone()
    
    # 가림막 만들기
    probability_matrix = torch.full(input_ids.shape, mask_prob)
    masked_indices = torch.bernoulli(probability_matrix).bool()
    
    # 특별 토큰은 가리지 않는다
    labels[~masked_indices] = -100
    
    # 80%는 [MASK]
    indices_replaced = torch.bernoulli(torch.full(input_ids.shape, 0.8)).bool() & masked_indices
    input_ids[indices_replaced] = mask_token_id
    
    # 10%는 무작위
    indices_random = torch.bernoulli(torch.full(input_ids.shape, 0.5)).bool() & masked_indices & ~indices_replaced
    random_words = torch.randint(vocab_size, input_ids.shape)
    input_ids[indices_random] = random_words[indices_random]
    
    # 10%는 그대로
    return input_ids, labels
```

#### 구간 망가뜨리기 (T5)

T5를 비롯한 인코더-디코더 모형이 쓴다.

이어진 구간을 파수 토큰으로 바꾼다.

```
Input:  "Thank you <X> to your party <Y> week."
Target: "<X> for inviting me <Y> last"
```

```python
def create_span_corruption_data(input_ids, sentinel_start_id, mean_span_length=3, noise_density=0.15):
    """구간 망가뜨리기 학습 데이터를 만든다(간추림)."""
    seq_len = input_ids.size(1)
    num_masked = int(seq_len * noise_density)
    
    # 구간의 시작을 고른다
    num_spans = max(1, num_masked // mean_span_length)
    
    # 망가진 입력과 목표를 만든다
    # (온전한 구현에는 구간을 조심스레 골라야 한다)
    pass
```

#### 접두 언어 모형화

양방향 접두와 인과 이어 가기를 아우른다.

$$
\mathcal{L} = -\sum_{t > L_{\text{prefix}}} \log P(x_t | x_1, \ldots, x_{t-1})
$$

UL2와 일부 인코더-디코더 모형이 쓴다.

#### 바뀐 토큰 찾기 (ELECTRA)

바뀐 토큰을 찾아내는 판별기를 학습시킨다.

$$
\mathcal{L} = -\sum_{t=1}^{T} \left[ y_t \log D(x_t) + (1-y_t) \log(1 - D(x_t)) \right]
$$

여기서 생성기가 바꾼 토큰이면 $y_t = 1$이다.

```python
class ELECTRA(nn.Module):
    def __init__(self, generator, discriminator):
        super().__init__()
        self.generator = generator  # 작은 가린 언어 모형
        self.discriminator = discriminator  # 주 모형
    
    def forward(self, input_ids, masked_indices):
        # 생성기가 가린 토큰을 맞힌다
        gen_logits = self.generator(input_ids)
        
        # 바꿔 넣을 것을 뽑는다
        with torch.no_grad():
            gen_probs = F.softmax(gen_logits, dim=-1)
            sampled = torch.multinomial(gen_probs.view(-1, gen_probs.size(-1)), 1)
            sampled = sampled.view(input_ids.shape)
        
        # 가린 자리를 바꾼다
        corrupted = input_ids.clone()
        corrupted[masked_indices] = sampled[masked_indices]
        
        # 판별기가 어느 것이 바뀌었는지 맞힌다
        disc_logits = self.discriminator(corrupted)
        labels = (corrupted != input_ids).float()
        
        return disc_logits, labels
```

#### 잡음 없애기 목표

#### 문서 돌리기
문서를 돌리고 얼마나 돌렸는지 맞힌다.

#### 문장 뒤섞기
문장을 뒤섞고 순서를 되살린다.

#### 토큰 지우기
토큰을 지우고 본디 수열을 맞힌다.

#### 견주기

| 목표 | 구조 | 양방향 | 잘 맞는 일 |
|-----------|--------------|---------------|----------|
| 인과 언어 모형 | 디코더 | 아니다 | 생성 |
| 가린 언어 모형 | 인코더 | 그렇다 | 이해 |
| 구간 망가뜨리기 | 인코더-디코더 | 접두는 그렇다 | 수열 대 수열 |
| ELECTRA | 인코더 | 그렇다 | 효율적인 사전 학습 |

#### 잡음 없애기 섞기 (UL2)

여러 목표를 아우른다.

```python
def ul2_objective(input_ids, mode='R'):
    """
    UL2의 잡음 없애기 섞기.
    
    방식:
    - R: 보통 잡음 없애기 (T5처럼)
    - S: 차례 잡음 없애기 (접두 언어 모형)
    - X: 극단 잡음 없애기 (많이 망가뜨림)
    """
    if mode == 'R':
        # 15% 망가뜨리기, 평균 구간 3
        return span_corruption(input_ids, 0.15, 3)
    elif mode == 'S':
        # 접두 언어 모형 방식
        return prefix_lm(input_ids)
    elif mode == 'X':
        # 50% 망가뜨리기, 평균 구간 32
        return span_corruption(input_ids, 0.50, 32)
```

#### 간추림

사전 학습 목표가 모형의 능력을 빚는다.

1. **인과 언어 모형**: 생성에 가장 좋고 학습이 간단하다
2. **가린 언어 모형**: 이해에 가장 좋고 양방향이다
3. **구간 망가뜨리기**: 효율적이고 수열 대 수열에 좋다
4. **ELECTRA**: 표본을 아끼고 판별력이 세다
5. **UL2**: 두루 쓰이며 여러 목표를 아우른다

#### 참고 문헌

1. Devlin, J., et al. (2019). "BERT: Pre-training of Deep Bidirectional Transformers."
2. Raffel, C., et al. (2020). "Exploring the Limits of Transfer Learning with T5."
3. Clark, K., et al. (2020). "ELECTRA: Pre-training Text Encoders as Discriminators."
4. Tay, Y., et al. (2022). "UL2: Unifying Language Learning Paradigms."

## 연습문제

**연습문제 1.**
T5의 '글에서 글로 옮기는' 틀을 설명하고 그것이 서로 다른 자연어 처리 과제를 어떻게 하나로 모으는지 밝혀라.

??? success "연습문제 1 풀이"
    T5는 모든 과제를 글 입력에서 글 출력으로 놓는다. 분류('classify: ...')는 이름표 문자열을, 번역('translate: ...')은 목표 언어의 글을, 요약('summarize: ...')은 요약 글을 낸다. 이 하나로 모은 꼴 덕분에 모든 과제에 모형 구조 하나와 학습 절차 하나를 쓸 수 있다.

---

**연습문제 2.**
T5의 인코더-디코더 구조와 GPT의 디코더 전용 구조를 견주어라.

??? success "연습문제 2 풀이"
    T5(인코더-디코더)는 인코더가 입력을 양방향으로 처리하고 디코더가 인코더에 교차 주의를 하며 출력을 자기 회귀로 만든다. 입력과 출력이 뚜렷이 구분되는 과제(번역, 요약)에 가장 좋다. GPT(디코더만)는 모든 것을 자기 회귀로 처리하여 더 간단하지만 입력에 양방향으로 주의할 수 없다. 디코더 전용이 더 잘 커지고 아주 큰 규모에서는 이쪽이 판친다.

---

**연습문제 3.**
T5는 어떤 사전 학습 목표를 쓰며 BERT의 MLM과 어떻게 다른가?

??? success "연습문제 3 풀이"
    T5는 '구간 망가뜨리기'를 쓴다. 이어진 토큰 구간을 파수 토큰으로 바꾸고 본디 구간을 맞힌다. 이를테면 'The <X> brown <Y>'에서 '<X> quick <Y> fox'를 맞힌다. 구간을 맞히면 맥락이 더 넉넉하고 맞혀야 할 횟수가 줄어 BERT의 토큰별 가리기보다 효율적이다.

---

**연습문제 4.**
T5 논문의 규모 실험과 그 핵심 발견을 설명하라.

??? success "연습문제 4 풀이"
    Raffel 외(2020)는 (1) 구조(인코더-디코더와 디코더 전용), (2) 사전 학습 목표(언어 모형, 가린 언어 모형, 구간 망가뜨리기), (3) 데이터셋(C4의 변형들), (4) 규모(6000만에서 110억)를 하나하나 견주었다. 핵심 발견은 지도 과제에서 인코더-디코더가 조금 낫고, 구간 망가뜨리기가 가장 좋은 목표이며, 데이터와 계산이 늘면 한결같이 도움이 되고, C4가 두루 쓸 만한 말뭉치라는 것이다.
